# REL_Editor
Open source HTML &amp; CSS Editor Elementor like.

See `REL_EDITOR/docs/README.md` for setup and usage.

Contribution rule: every new editor UI control must include a `data-tooltip-key` and a matching entry in `REL_EDITOR/editor/tooltips_registry.js`.
